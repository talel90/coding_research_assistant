venv activiation -> .\.venv\Scripts\Activate.ps1


after intalling the whole pachges in the venv froze it to create requirements.txt -> pip freeze > requirements.txt

auto_commmitment -> bash autocommit.sh